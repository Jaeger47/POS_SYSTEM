Quick reminder on how to launch it:
Open your terminal in the folder containing these files.

If it's your first time, run: npm init -y and then npm install express cors

Start the helper server by running: node server.js

Now, open your index.html in your web browser.